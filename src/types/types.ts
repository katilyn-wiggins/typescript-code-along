export interface Character {
  id: number;
  name: string; 
  affiliation: string; 
  origin: string;
  race: string; 
  imageUrl: string; 
} 

export interface ListCharacter {
  id: number; 
  name: string;
  imageUrl: string;
}